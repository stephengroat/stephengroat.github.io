name: CI

on:
  push:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron:  '0 1 * * *'

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - uses: nanasess/setup-chromedriver@master
    - name: Run brewfile
      # TODO https://github.com/Homebrew/linuxbrew-core/issues/20914
      run: brew upgrade && brew bundle
    - name: Setup env
      run: |
        echo "::add-path::$HOME/.rbenv/bin"
        echo "::add-path::$HOME/.rbenv/shims"
        echo "::add-path::$HOME/.tfenv/bin"
    - name: terraform fmt
      # TODO determine why tfenv autoinstall not working
      run: tfenv install && terraform fmt -diff -check
    - name: Run a multi-line script
      env:
        DISPLAY: :99
      run: |
        sudo gem install bundler
        bundle install
        xvfb-run bundle exec rake
